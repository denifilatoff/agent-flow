---
description: Select the bugfixer entry agent for reproduced failures.
applyTo: "**/*"
---

Use `bugfixer` as this package's only entry agent to fix a reproduced failure and add a direct regression check.
